# Legacy static prototype

The original dependency-free single-page prototype, kept for reference. The
maintained application now lives in `/src` (React + Vite, wrapped by
Capacitor and Tauri) — see the repository root `README.md`.
